# docs/20260831_v1_GATE210_INSTALL.md

# Gate 2.10 — manual layer (install runbook)

Bioindustry Intelligence Platform · Implementation Plan v15 row 2.10 · 2026-08-31.
Scope approved 2026-08-31 with the storage ruling applied: the database is the
sole store for every operational fact; git carries no row content; machines
reconcile by the hashed snapshot; `manual export`/`manual load` is the rebuild
path for all hand-entered tables. Entry commit on `jason/refactor`: 097ca57.

## 1. Package installs
None.

## 2. New files (deploy destination on line 1 of each)
| Download | Deploys to |
|---|---|
| `manual.py` | `src\biointel\manual.py` |
| `test_manual.py` | `tests\unit\test_manual.py` |

## 3. Replaced files (full-file replacements; deploy path on line 1)
| Download | Replaces | Change |
|---|---|---|
| `schema_v904.py` (replacement of L2's v903) | `src\biointel\schema.py` | SCHEMA_VERSION 0.7; the three manual tables un-planned and declared live with keys and date types (columns unchanged from their planned declarations); typed-edge columns on `relationships` remain the already-declared optional set (`type`, `date`, `source`), completing the 0.1 deferral by declaration — writers arrive with the L3 adapters; nothing else changed |
| `cli_v904.py` (replacement of L2's v903) | `src\biointel\interfaces\cli.py` | `manual` command group (64th command: add-entity, add-attribute, add-note, list, validate, export, load); dossier-seed help line marked LEGACY; nothing else changed |
| `dossier_v004.py` (replacement of v003) | `src\biointel\dossier.py` | SEED content marked LEGACY per the storage ruling (marked, not deleted); the span-verification machinery stays live for L3; no functional change |
| `test_schema_v901.py` (first replacement of the pre-L1 file) | `tests\unit\test_schema.py` | one assertion updated: manual_notes planned→absent (live since 2.10), benchmarks asserted planned |
| `test_store_v901.py` (first replacement of the pre-L1 file) | `tests\unit\test_store.py` | same one-assertion update |

## 4. Surgical edits
None (the two test updates ship as full files above).

## 5. Migration commands
None. No table is rewritten; the manual tables are created on first use
(`manual add-*`) or by `manual load`, which writes every hand table
(manual trio + eight dossier tables) from the export file.

## 6. Automated test command
`& "<root>\.venv\Scripts\python.exe" -m pytest -q tests/unit` — expected `103 passed`
(95 prior + 8 gate-2.10 tests: declarations, refusals, upsert+provenance,
visible precedence, entities/notes, sweep, export/load round trip incl. a
dossier table with wipe-and-restore, clean miss).

## 7. Live verification (exit criteria)
1. pytest 103; ruff clean on the gate files.
2. Round trip on the real database: one manual attribute recorded, `manual list`
   shows it with provenance, an invalid target refused, `manual validate` 0
   problems.
3. `manual export`: 17 rows (16 dossier + 1 attribute) across 6 tables to
   `data\manual\hand_data.json`; `manual load`: same 17 back with a recorded
   run — proving the rebuild path carries the dossier.
4. `validate`: `38 conformant, 0 with violations, 8 absent, 1 planned (47 declared)`
   (the three manual tables conformant after the load; benchmarks the last
   planned table).
5. Regression: thirteen-hash loop — thirteen MATCH, PROBLEMS 0 (~2 h; no model,
   reader, or export path touched; merged_rows has no consumer at this gate by
   design, so fingerprints hold by construction).

## 8. What was verified in the dry-run (container, 2026-08-31, Python 3.12 as before)
- pytest 103; ruff check and format clean on the seven gate files.
- Real-CLI fixture run: add-attribute recorded with provenance; undeclared
  column refused with the reason; list/validate clean; export 2 rows / 2
  tables; load 2 rows with run recorded; schema validate shows `1 planned`.
- Precedence proven in tests: merged_rows returns the manual value over the
  machine value with the applied override listed; nothing consumes merged_rows
  yet, per scope.
- Not verifiable in the container: your row counts and the fingerprints; step 7
  is the proof.

## 9. Decisions applied
- Storage ruling 2026-08-31: database sole store; no row content in git;
  snapshot is the sole cross-machine reconciliation; export/load is rebuild
  insurance under `data\` (git-ignored). The dossier SEED in dossier.py is
  marked LEGACY accordingly.
- P5: the layer is a general override (any overridable table.column), not
  M&A-specific; targets validated against the declared schema.
- Precedence is visible: merged reads return the applied-overrides list;
  consumers opt in at their own gates (L3 review queue first).

## 10. Deferred, named here
- Typed-edge population on `relationships` (writers): L3 adapters.
- First merged_rows consumer: L3 review queue.
- Snapshot procedure note (export file rides in `data\`, so `freeze` snapshots
  carry it automatically): recorded in the gate-close docs.
